server.port=8000
# 身份信息
app.id=SampleApp
# Meta Server 地址
apollo.meta=http://localhost:8080
# 项目启动的bootstrap阶段，向Spring容器注入配置信息
apollo.bootstrap.enabled=true
# 注入命名空间
apollo.bootstrap.namespaces=application

username=luozy

redis.cache.host=192.168.1.1
redis.cache.port=2181
